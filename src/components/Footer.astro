---
const today = new Date();
---

<footer>
	<p>&copy; {today.getFullYear()} YouUsHi. All rights reserved.</p>
	<p style="margin-top: 0.75rem; font-size: 0.9rem; opacity: 0.75; font-style: italic;">
		You joining us. We're saying hello.
	</p>
</footer>
<style>
	footer {
		padding: 3rem 1.5rem;
		background: rgba(var(--white), 0.95);
		backdrop-filter: blur(10px);
		color: rgb(var(--gray));
		text-align: center;
		border-top: 1px solid rgba(var(--gray-light), 0.8);
		margin-top: 4rem;
	}

	footer p {
		margin: 0;
		font-size: 0.9rem;
	}

	footer p:first-child {
		margin-bottom: 0.25rem;
	}

	.social-links {
		display: flex;
		justify-content: center;
		gap: 1rem;
		margin-top: 1.5rem;
	}

	.social-links a {
		text-decoration: none;
		color: rgb(var(--gray));
		transition: all 0.2s ease;
		padding: 0.5rem;
		border-radius: 4px;
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.social-links a:hover {
		color: var(--accent);
		background-color: rgba(var(--accent-light), 0.1);
		transform: translateY(-2px);
	}

	@media (max-width: 720px) {
		footer {
			padding: 2rem 1rem;
			margin-top: 3rem;
		}

		footer p {
			font-size: 0.875rem;
		}

		.social-links {
			gap: 0.75rem;
			margin-top: 1.25rem;
		}
	}
</style>
